pipeline {
  agent any
  stages {
  stage("Clean") {
		steps {
			sh "dotnet clean"
		}
  }
  stage("Build") {
		steps {
			sh "dotnet build -c Release -warnaserror"
		}
  }
  stage("Test") {
		steps {
			sh "dotnet test"
		}
  }
}
}
